name: Automatizacion Diario COES RER

on:
  schedule:
    # 22:00 UTC equivale a las 17:00 (5:00 PM) hora de Perú (GMT-5)
    - cron: '0 22 * * *'
  workflow_dispatch: # Permite ejecutar el script manualmente desde el botón de GitHub cuando quieras

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Permiso crucial para que el bot pueda guardar el archivo actualizado

    steps:
    - name: Clonar repositorio actual
      uses: actions/checkout@v4

    - name: Configurar entorno de Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Instalar librerías requeridas
      run: |
        python -m pip install --upgrade pip
        pip install pandas openpyxl requests matplotlib seaborn

    - name: Ejecutar script extractor
      run: python script.py

    - name: Guardar y subir cambios al repositorio de GitHub
      run: |
        git config --global user.name "COES Bot"
        git config --global user.email "actions@github.com"
        # Agregamos la carpeta data/ (o data/data.json) para que se incluya en el commit:
        git add historico_generacion_rer.csv perfil_generacion_rer.png data/
        # Solo hacer commit si los archivos realmente cambiaron para evitar errores
        git diff --quiet && git diff --staged --quiet || (git commit -m "Actualización automática RER, Gráfico y JSON - $(date +'%Y-%m-%d')" && git push)
